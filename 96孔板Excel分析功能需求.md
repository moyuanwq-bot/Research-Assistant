# 96 孔板 Excel 数据分析功能需求

## 1. 功能目标

为网页版 96 孔板工具增加多时间点吸光度数据分析功能。用户导入一个包含多个工作表的 Excel 文件，系统读取各时间点的 96 孔板数据，展示每个孔的数据变化，并导出包含明细、热图和折线图的分析工作簿。

全部数据在浏览器本地处理，不上传服务器，继续保持 GitHub Pages 静态部署和即开即用的使用方式。

## 2. 已确定需求

- 本次只修改网页版项目。
- 输入为一个 Excel 工作簿，工作簿中包含多个工作表。
- 每个工作表代表同一块 96 孔板的一个时间点。
- 不同工作表采用相同板布局，同一孔位直接对应，例如 A1 与 A1 比较。
- 需要兼容酶标仪导出的原始 Excel，而不是强制用户套用固定模板。
- 实际解析规则需要根据一份脱敏后的真实仪器导出样例确定。
- 时间优先从工作表名称自动提取，识别结果在生成报告前允许用户检查和修改。
- 用户通过 8×12 孔板网格选择需要绘制变化曲线的孔位。
- 孔位选择界面应提供全选、清空、按行选择和按列选择。
- 纵坐标为“吸光度（OD）”；吸光度本身不以 nm 为单位。
- 检测波长单独填写，例如 450 nm，并显示在报告信息和图表标题中。
- 每个时间点同时生成可编辑的 Excel 单元格热图和嵌入工作簿的 PNG 热图。
- 对用户选择的孔位生成时间—吸光度折线图。
- 最终输出一个 Excel 分析工作簿。
- 折线图以高清 PNG 形式嵌入 Excel，同时保留完整数值明细，便于用户在 Excel 中二次分析或重新作图。

## 3. 建议操作流程

1. 用户点击“导入多时间点 Excel”。
2. 浏览器读取工作簿并列出所有候选工作表。
3. 系统从工作表名称识别时间，并展示识别结果供用户修改。
4. 用户填写检测波长及时间单位。
5. 系统解析各工作表的 8×12 吸光度矩阵并预览。
6. 若存在无法识别的工作表、孔位或非数值内容，系统明确指出位置和原因。
7. 用户在 96 孔板网格中选择需要绘图的孔位。
8. 系统生成热图和折线图预览。
9. 用户点击导出，下载一个完整的 Excel 分析工作簿。

## 4. 输出工作簿结构

### 4.1 实验信息

- 原始文件名
- 报告生成时间
- 检测波长（nm）
- 时间单位
- 原工作表名称、识别后的时间值及排序结果
- 数据解析警告或备注

### 4.2 数据明细

采用长表结构保存全部原始读数，至少包含以下列：

- 时间
- 时间单位
- 孔位
- 吸光度（OD）
- 来源工作表

### 4.3 板式热图

- 每个时间点生成一张 8×12 数值矩阵。
- 行标为 A–H，列标为 1–12。
- 使用统一色阶表示吸光度高低，保证不同时间点可以直接比较。
- 单元格保留原始数值并应用条件格式。
- 同时生成 PNG 热图并嵌入工作簿。

### 4.4 变化曲线

- 仅为用户选择的孔位作图。
- 横坐标为时间，按数值大小排序。
- 纵坐标为吸光度（OD）。
- 图标题包含检测波长，例如“选定孔位吸光度变化（450 nm）”。
- 每个孔位使用独立曲线和图例标识。
- 图表下方或相邻区域保留对应的时间—吸光度数值表。

## 5. 数据校验要求

- 每个有效时间点最多读取 A1–H12 共 96 个孔位。
- 工作表之间按孔位坐标一一对应，不自动按样品名称重新匹配。
- 时间识别后必须允许人工修正，避免把批次号或日期误当成实验时间。
- 非数值、空白、重复孔位、矩阵方向不明和数据区域识别失败都需要明确提示。
- 不应静默丢弃异常工作表或异常孔位。
- 导出前显示解析摘要，包括成功时间点数、有效读数数和异常数。
- 导出的公式不得出现 `#REF!`、`#DIV/0!`、`#VALUE!`、`#N/A` 或 `#NAME?`。

## 6. 技术方向

- 使用 SheetJS 在浏览器端读取和解析 `.xlsx` 文件。
- 使用 ExcelJS 生成带格式、条件色阶和嵌入图片的分析工作簿。
- 使用 Canvas 配合 Chart.js 生成热图和折线图 PNG。
- 不新增后端服务，不上传实验文件或读数。
- 第三方依赖优先随项目静态托管，避免核心功能依赖外部 CDN 可用性。
- 折线图作为高清 PNG 嵌入工作簿；完整数据保留在工作表中，供后续编辑和重新绘图。

## 7. 尚待确认

以下内容需在获得真实 Excel 样例后确认，不能在缺少样例时武断实现：

- 酶标仪品牌、型号及导出格式。
- 96 孔数据在工作表中的起始位置、方向和周边标题结构。
- 一个工作表中是否可能存在多次读取结果或多个 96 孔矩阵。
- 空白孔、缺失值、超量程值和文本标记的实际表现形式。
- 工作表时间命名规则，例如 `0h`、`30min`、`Day1` 或其他格式。
- 无法识别时间或完整矩阵时，是阻止生成还是允许用户手动指定数据区域。
- 检测波长是整个工作簿统一填写，还是允许各工作表分别填写。

## 8. 实施前置材料

用户需提供至少一份脱敏后的真实酶标仪 Excel 导出文件。可以删除样品名称和其他敏感信息，但应保留原始工作表布局、标题、合并单元格、孔位标记以及数值格式，以便建立可靠的解析和错误提示规则。
